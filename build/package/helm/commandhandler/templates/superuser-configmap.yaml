{{- if .Values.superusers.emails }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "commandhandler.fullname" . }}-superusers
data:
  emails: {{ join "," .Values.superusers.emails | quote }}
{{- end }}
