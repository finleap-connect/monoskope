{{ if eq .Values.storeDatabase.existingSecret "" }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "eventstore.fullname" . }}-db
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "eventstore.labels" . | nindent 4 }}
    {{- with (.Values.labels | default .Values.global.labels) }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
stringData:
  DB_URL: {{ .Values.storeDatabase.url }}
{{ end }}