apiVersion: v1
kind: Service
metadata:
  name: {{ include "monoskope.fullname" . }}
  labels:
    {{- include "monoskope.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "monoskope.selectorLabels" . | nindent 4 }}
