{{- if and .Values.dex.config.existingSecret .Values.dex.certs.grpc.use }}
apiVersion: cert-manager.io/v1alpha2
kind: Issuer
metadata:
  name: {{ include "monoskope.fullname" . }}-selfsigning-issuer
  labels:
    {{- include "monoskope.labels" . | nindent 4 }}
spec:
  selfSigned: {}
{{- end }}