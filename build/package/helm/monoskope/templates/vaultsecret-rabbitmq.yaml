{{- if .Values.rabbitmq.auth.existingErlangSecret }}
---
apiVersion: vault.finleap.cloud/v1alpha1
kind: VaultSecret
metadata:
  name: {{ include "monoskope.fullname" . }}-erlang-cookie
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "monoskope.labels" . | nindent 4 }}
    {{- with (.Values.labels | default .Values.global.labels) }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  secretName: {{ .Values.rabbitmq.auth.existingErlangSecret }}
  data:
    - name: rabbitmq-erlang-cookie
      generator:
        name: password
        args: [32]
      location:
        path: app/{{ .Release.Namespace }}/monoskope/rabbitmq
        field: erlangCookie
---
{{- end }}
{{- if .Values.rabbitmq.loadDefinition.enabled }}
apiVersion: vault.finleap.cloud/v1alpha1
kind: VaultSecret
metadata:
  name: {{ include "monoskope.fullname" . }}-rabbitmq-definitions
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "monoskope.labels" . | nindent 4 }}
    {{- with (.Values.labels | default .Values.global.labels) }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  secretName: {{ .Values.rabbitmq.loadDefinition.existingSecret }}
  data:
    - name: adminPassword
      generator:
        name: password
        args: [32]
      location:
        path: app/{{ .Release.Namespace }}/monoskope/rabbitmq
        field: adminPassword
    - name: rabbitmq-definitions.json
      variables:
      - name: adminPassword
        location:
          path: app/{{ .Release.Namespace }}/monoskope/rabbitmq
          field: adminPassword
      template: |-
        {
          "users": [
            {
              "name": {{ .Values.rabbitmq.auth.username | quote }},
              "password": {{`{{ .adminPassword | quote }}`}},
              "tags": "administrator"
            }
          ],
          "vhosts": [{"name": "/"}],
          "permissions": [
            {
              "user": {{ .Values.rabbitmq.auth.username | quote }},
              "vhost": "/",
              "configure": ".*",
              "read": ".*",
              "write": ".*"
            }
          ],
          "parameters": [],
          "policies": [
              {
                  "vhost": "/",
                  "name": "ha",
                  "pattern": "",
                  "definition": {
                      "ha-mode": "exactly",
                      "ha-params": {{ add1 (div .Values.rabbitmq.replicaCount 2) }},
                  }
              }
          ],
          "queues": [],
          "exchanges": [],
          "bindings": []
        }
---
{{- end }}
